// node版本
const REQUIRED_NODE_VERSION = ">=8.9"

// 废弃的版本
const EOL_NODE_MAJORS = ["8.x", "9.x", "11.x", "13.x"]

// 默认脚手架目录
const DEFAULT_CLI_HOME = ".eqshow-cli"

module.exports = {
  REQUIRED_NODE_VERSION,
  EOL_NODE_MAJORS,
  DEFAULT_CLI_HOME
}
